import React from 'react';

const LoadingTextInput = () => {
    return(
        <div className="br3 mv1 w-50 center">
                <p style={{color: "white", fontWeight:"bold"}}>Loading... Please be patient. This may take a while depending on the size of the input</p>
        </div>
    );
}

export default LoadingTextInput